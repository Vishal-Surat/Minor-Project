{
  "name": "security-dashboard",
  "version": "1.0.0",
  "description": "Network Security Dashboard with Real-time Monitoring",
  "main": "index.js",
  "scripts": {
    "start": "npm run start --prefix backend ",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "keywords": [
    "security",
    "dashboard",
    "react",
    "nodejs",
    "mongodb"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}